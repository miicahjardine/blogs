Compute Resource Deployment is dependent on the two following resources.

Templates for the below resources exist in the bluebank ARM template store

1. DiagnosticStorage.   Must be deployed in RG-$POC-DiagnosticStorage-$ENV-$LocationShort.
2. VirtualNetwork.      Must be deployed in RG-$POC-Network-$ENV-$locationShort.